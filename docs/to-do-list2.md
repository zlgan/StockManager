# 修改要求

# ## 功能需求
### 商品类别管理

- [x] 添加，编辑，删除时页面显示loading提示，参考供应商管理提交页面

- [x] ### 供应商管理

- [x] 1. 查询页面的浮动添加按钮样式，应该跟客户管理页面的的添加按钮风格保持一致

- [x] 2. 查询文本框供应商名称和供应商编号，点击文本框之后里面的提示文字显示不全，请修复

- [x] 3. 新增供应商页面中供应商编号，不要自动生成，让用户自己填写，保存的时候检查不能重复

### 产品列表

- [x] 1. 查询页面的浮动添加按钮样式，应该跟客户管理页面的的添加按钮风格保持一致

### 入库登记

- [x] 1. 单据号没有默认值，保存后生成，规则类似出库登录的单据号

- [x] 2. 产品信息删除按钮，要确认后再删除

- [x] 3. 不允许在一次入库中提交多个相同的产品

### 单据详情

- [x] 1. 只允许修改入库数量和入库单价
- [x] 2. 单据不允许删除，产品不允许删除
- [x] 3. 产品型号后面的扫描按钮不需要显示
- [x] 4. 不显示添加产品按钮
- [x] 5. 供应商下拉列表数据显示不正确，请从数据库中读取，并根据入库时的选择显示正确的供应商
- [ ] 编辑出库单，要正确显示客户信息，并且去掉提示"选择供应商"的逻辑
- [ ] 编辑单据提交之后应该跳转回原来的页面


### 出库登记

- [x] 1. 输入产品型号之后，产品图片没有正确加载

- [x] 2. 产品信息删除按钮，要确认后再删除

- [x] 3. 点击添加产品之后，新的产品应该现在在上面。应该跟入库时的逻辑保持一致

- [x] 4. 不允许在一次出库中提交多个相同的产品

### 新增客户

- [x] 1. 客户编号应该，让用户自己填写，保存的时候检查不能重复

- [x] 2. 售后周期下来列表显示不全，请调整长度

### 库存盘点

- [x] 1. 库存盘点-->产品详情-->入库/出库， 通过此途径进入出库或者入库登记页面的，应该直接在相应的页面加载该产品信息不要再次输入

### 单据查询

- [x] 1. 供应商和客户应该从数据库中读取，而不是使用模拟数据


### 员工详情
- [ ] 修改员工信息时用户名不允许修改,如果没有填写密码则密码保持不变
- [ ] 密码长度要求6-20位

### 统计报表
- [ ] 对于调整入库单据要需要减库存的场景，库存变动记录出现+-8，应该去掉前面的+号

### 其他可选优化
- [ ] 所有页面点击图片支持看大图
- [ ] 小程序页面文字支持选择和复制
- [ ] 所有的查询、搜索按钮均应该使用"客户管理"页面的样式
- [ ] 做一个实验页面，将公共样式提取出来测试
- [ ] 系统设置的功能还需要测试
- [ ] 所有的列表页实现左滑出现删除按钮
- [ ] 所有的取消按钮都应该参考 "编辑客户"的样式
- [ ] 系统设置页面，动画时间过长，需要调整
- [ ] 用户注册密码方案可能优化方案
```js
// 安装：npm install bcrypt
const bcrypt = require('bcrypt');
const saltRounds = 12; // 强度因子，值越高越安全但越慢
async function savePassword(plainPassword) {
    // 哈希并自动生成安全盐
    const hash = await bcrypt.hash(plainPassword, saltRounds);
    // hash字符串已包含算法、成本因子、盐和最终哈希值，例如：$2b$12$...盐...哈希值
    return {
        passwordHash: hash
        // 注意：bcrypt的盐已包含在hash中，无需单独存储
    };
}
async function verifyPassword(plainPassword, storedHash) {
    return await bcrypt.compare(plainPassword, storedHash);
}
```

## 非功能事宜

### devops

### 上线流程
- [ ] 如何创建两个免费环境？https://docs.cloudbase.net/faq/env
- [ ] 云函数开发扫盲 https://blog.csdn.net/x15514104477/article/details/145753884

